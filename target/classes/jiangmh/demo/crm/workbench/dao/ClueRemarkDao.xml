<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jiangmh.demo.crm.workbench.dao.ClueRemarkDao">
	<select id="getRemarkListByCId" resultType="ClueRemark">
        select * from tbl_clue_remark where clueId = #{clueId}
    </select>

    <delete id="deleteById">
        delete from tbl_clue_remark where id=#{id}
    </delete>

    <select id="getCountsByCid" resultType="Integer">
        select count(*) from tbl_clue_remark where clueId=#{id}
    </select>

    <delete id="deleteByCid">
        delete from tbl_clue_remark where clueId=#{id}
    </delete>

    <update id="updateRemark">
        update tbl_clue_remark set noteContent=#{noteContent} where id=#{id}
    </update>

    <insert id="saveRemark">
        insert into tbl_clue_remark(
            id,
            noteContent,
            createBy,
            createTime,
            editFlag,
            clueId
        )values(
            #{id},
            #{noteContent},
            #{createBy},
            #{createTime},
            #{editFlag},
            #{clueId}
        )
    </insert>
</mapper>


































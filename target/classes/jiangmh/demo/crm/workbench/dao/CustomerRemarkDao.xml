<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jiangmh.demo.crm.workbench.dao.CustomerRemarkDao">
	<insert id="save" parameterType="list">
        insert into tbl_customer_remark(
            id,
            noteContent,
            createBy,
            createTime,
            editFlag,
            customerId
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id},#{item.noteContent},#{item.createBy},#{item.createTime},#{item.editFlag},#{item.customerId})
        </foreach>
    </insert>

    <select id="getRemarkListByCusid" resultType="map">
        select
        r.noteContent,
        r.createBy,
        r.createTime,
        r.editBy,
        r.editTime,
        r.id,
        r.editFlag,
        c.name
        from tbl_customer_remark r
        join tbl_customer c
        on r.customerId = c.id
        where r.customerId=#{id}
    </select>

    <update id="updateRemark">
        update tbl_customer_remark set
        noteContent=#{noteContent},
        editBy=#{editBy},
        editTime=#{editTime},
        editFlag=#{editFlag}
        where id=#{id}
    </update>

    <delete id="deleteRemark">
        delete from tbl_customer_remark where id=#{id}
    </delete>

    <insert id="addRemark">
        insert into tbl_customer_remark(
        id,
        noteContent,
        createBy,
        createTime,
        editBy,
        editTime,
        editFlag,
        customerId
        )values(
        #{id},
        #{noteContent},
        #{createBy},
        #{createTime},
        #{editBy},
        #{editTime},
        #{editFlag},
        #{customerId}
        )
    </insert>
</mapper>

































